<h1><%= @article.title %></h1>
<p><%= @article.content %></p>

<%= link_to "Delete", article_url(@article),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

      <br>
      <br>
<%= link_to "Update", edit_article_path(@article) %>

